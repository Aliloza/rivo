# Fish Age Prediction Benchmark

This repository is a benchmark for predicting the age of fish using various models and datasets.

## Requirements
- Python 3.x
- numpy
- pandas
- scikit-learn
- TensorFlow or PyTorch (optional, depending on the model)

## Installation
You can install the required packages using:
```
pip install -r requirements.txt
```